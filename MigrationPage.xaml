<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MigrationTool.MigrationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MigrationTool"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Padding="40" RowSpacing="20">
        <StackPanel Grid.Row="0" Spacing="10">
            <InfoBar x:Name="StatusInfoBar" IsOpen="False" IsClosable="True" HorizontalAlignment="Stretch" />
            <TextBlock Text="Migration Settings" Style="{StaticResource TitleTextBlockStyle}"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Spacing="10" VerticalAlignment="Bottom">
            <ProgressBar x:Name="MigrationProgress" Value="0" IsIndeterminate="False" Height="10"/>
            <Button Content="Start Migration" Style="{StaticResource AccentButtonStyle}" 
             HorizontalAlignment="Center" Width="200" Height="40" Click="Test_Click"/>
        </StackPanel>
    </Grid>
</Page>
